interface:
  display_name: "BugHunter: Report"
  short_description: "Run the explicit report workflow"
  default_prompt: "Use $codex-bughunter:workflow-report on an authorized target or finding."
policy:
  allow_implicit_invocation: false
