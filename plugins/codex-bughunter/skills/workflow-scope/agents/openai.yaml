interface:
  display_name: "BugHunter: Scope"
  short_description: "Run the explicit scope workflow"
  default_prompt: "Use $codex-bughunter:workflow-scope on an authorized target or finding."
policy:
  allow_implicit_invocation: false
