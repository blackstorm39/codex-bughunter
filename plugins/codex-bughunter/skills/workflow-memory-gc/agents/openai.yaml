interface:
  display_name: "BugHunter: Memory Gc"
  short_description: "Run the explicit memory-gc workflow"
  default_prompt: "Use $codex-bughunter:workflow-memory-gc on an authorized target or finding."
policy:
  allow_implicit_invocation: false
