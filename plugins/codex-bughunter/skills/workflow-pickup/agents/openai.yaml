interface:
  display_name: "BugHunter: Pickup"
  short_description: "Run the explicit pickup workflow"
  default_prompt: "Use $codex-bughunter:workflow-pickup on an authorized target or finding."
policy:
  allow_implicit_invocation: false
