interface:
  display_name: "BugHunter: Recon"
  short_description: "Run the explicit recon workflow"
  default_prompt: "Use $codex-bughunter:workflow-recon on an authorized target or finding."
policy:
  allow_implicit_invocation: false
