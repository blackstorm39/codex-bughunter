interface:
  display_name: "BugHunter: Chain"
  short_description: "Run the explicit chain workflow"
  default_prompt: "Use $codex-bughunter:workflow-chain on an authorized target or finding."
policy:
  allow_implicit_invocation: false
