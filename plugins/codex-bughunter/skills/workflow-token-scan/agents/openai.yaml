interface:
  display_name: "BugHunter: Token Scan"
  short_description: "Run the explicit token-scan workflow"
  default_prompt: "Use $codex-bughunter:workflow-token-scan on an authorized target or finding."
policy:
  allow_implicit_invocation: false
